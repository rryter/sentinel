name: Scoper Analysis

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run scoper
        uses: ./.github/actions/scoper-action  # Path to your action
        with:
          args: ''